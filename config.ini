[DEFAULT]

# Path to the input file containing city data (e.g., lat, lon, country, capital_city)
input_cities_file = input_cities.csv

# Path where the extracted weather data (including the API response) will be saved
extracted_weather_file = etl_files/extracted_weather.parquet

# Path where the transformed data (with additional columns) will be saved
transformed_weather_file = etl_files/transformed_weather.parquet

# Path where the final output will be saved
loaded_weather_file = etl_files/loaded_weather.parquet

# Pause duration (in seconds) between API requests
pause_duration = 1.0